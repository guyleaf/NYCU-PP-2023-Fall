--- test2.cpp
+++ test2.cpp
@@ -15,8 +15,10 @@
         for (int j = 0; j < N; j++)
         {
             /* max() */
-            c[j] = a[j];
-            if (b[j] > a[j]) c[j] = b[j];
+            if (b[j] > a[j])
+                c[j] = b[j];
+            else
+                c[j] = a[j];
         }
     }